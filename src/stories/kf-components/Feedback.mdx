import { Canvas, Meta } from "@storybook/addon-docs/blocks";

import * as FeedbackStories from "./Feedback.stories";

<Meta of={FeedbackStories} />

# Feedback

Use Feedback together with [Sonner](/story/shadncui-components-sonner--docs).

Give user a response using Sonner when either the positive or negative button has been clicked.

Giver user a response using Sonner when a comment has been submitted.

<Canvas of={FeedbackStories.Default} />
<Canvas of={FeedbackStories.Custom} />
