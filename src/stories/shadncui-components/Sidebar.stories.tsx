import type { Meta, StoryObj } from "@storybook/react-vite";
const meta = {
  title: "shadcnui-components/Sidebar",
  component: () => null,
  tags: ["autodocs"],
} satisfies Meta;

export default meta;
type Story = StoryObj;

export const Default: Story = {
  render: () => <div>work in progress</div>,
};
